{
  "name": "significa-style",
  "private": true,
  "npmClient": "yarn",
  "useWorkspaces": true,
  "version": "independent",
  "scripts": {
    "publish": "lerna publish"
  },
  "packages": [
    "packages/*"
  ],
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.19.0"
  }
}
